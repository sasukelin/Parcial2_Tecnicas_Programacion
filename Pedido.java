import java.util.List;
public class Pedido {

    private Cliente cliente;
    private List<ItemPedido> productosolicitado;



        public Pedido(Cliente cliente) {
            this.cliente = cliente;
            this.productosolicitado = new Arrylist<>;
        }
    public Pedido agregarPeido(Pedido pedido) {
        if (Pedido.size() < ) {
            Reserva reserva = new Reserva(this, pasajero, fechaReserva);
            Pasajeros.add(pasajero);
            return reserva;
        } else {
            return null;
        }


}
